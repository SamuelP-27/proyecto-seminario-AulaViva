// T3.1.3 - contar el número total de distracciones y la duración promedio de cada una

// Archivo: analizador_atencion.cpp

m.numEventosDistrac     = nEvt;
m.duracionMaxDistracS   = durMax;
m.duracionMediaDistracS = (nEvt > 0) ? (durTotal / nEvt) : 0.0;